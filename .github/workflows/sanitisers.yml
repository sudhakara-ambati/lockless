name: Memory Safety

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  memory-checks:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v4
      
    - name: Setup compiler
      uses: msys2/setup-msys2@v2
      with:
        msystem: MINGW64
        update: true
        install: mingw-w64-x86_64-gcc
      
    - name: Build with strict warnings
      shell: msys2 {0}
      run: |
        g++ -std=c++17 -Wall -Wextra -Wpedantic -Wshadow -I include -o test1.exe examples/test1.cpp
        
    - name: Check atomics
      shell: msys2 {0}
      run: |
        grep "memory_order" include/lockless.h